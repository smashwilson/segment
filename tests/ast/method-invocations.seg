# Different styles of method invocation.

# Explicit receiver + parenthesis arguments:
thing.method(1, 2, 3)

# Implicit receiver + parenthesis arguments:
method(3, 2, 1)

# Explicit receiver + space arguments:
file.puts 4 5 6

# Implicit receiver + space arguments:
puts 7 8 9

# Space arguments, with complex argument types:
blerb (7 - 8) {
  3 + 4
} (6 &and& 89)

# Empty parenthesis arguments
method()

# Method chaining
one().two().three()

# Parenthesis arguments are statements, so they can also use space invocation.
one(puts 3 4, gets 42)

# As are parenthetical expressions.
(foo 1).method(bar 2)
